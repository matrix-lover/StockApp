<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:StockApp"
       x:Class="StockApp.AppShell"
       Shell.FlyoutBehavior="Disabled">

  <Shell.Resources>
    <!-- здесь можно положить ресурсы, если нужно -->
    <Color x:Key="PageBackgroundColor">#121212</Color>
  </Shell.Resources>

  <!-- Открываем MainPage внутри Shell -->
  <ShellContent Title="Stocks" ContentTemplate="{DataTemplate local:MainPage}" />

</Shell>